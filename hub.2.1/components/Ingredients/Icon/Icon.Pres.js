
import styled from 'styled-components';
import Style from '../../../services/Style';
import Home from '../../Assets/Icons/IconHome';


const IconContainer = styled.span`
`;

const Icon = props => (
	<IconContainer>
		<Home />
	</IconContainer>
);

export default Icon;
